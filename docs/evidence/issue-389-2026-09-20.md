# Issue #389 installed-package evidence — 2026-09-20

This record covers the package lifecycle and installed-surface checks for
`luci-app-fwlive` 0.1.44-r1. The package hook is defined before LuCI's
`BuildPackage` invocation so both package formats carry the WAN-log baseline
restore logic.

SHA-256 pins below bind the named `out/` artifacts only; they do not claim
those IPK/APK files were built from the current cycle's `prerm` body. Packaged-hook
action dispatch is covered by host tests on #405
(`tests/fwlive-package-lifecycle.test.sh`). Rebuild and re-pin the artifacts
before treating the hashed packages as carrying this cycle's hook.

## Artifacts

- IPK: `out/x86_64/24.10.8/fwlive/luci-app-fwlive_0.1.44-r1_all.ipk`
  (`sha256 c6f355d005761ccc373ad51776b051db2ca0cfc7339fb37a13d2d12280c32891`)
- IPK: `out/aarch64_generic/24.10.8/fwlive/luci-app-fwlive_0.1.44-r1_all.ipk`
  (`sha256 cce847d6060db1ac54f6803f2f76bf5b9061a01d7800177c4a7b59ee2072e319`)
- APK: `out/x86_64/25.12.5/fwlive/luci-app-fwlive-0.1.44-r1.apk`
  (`sha256 2a15524a188930104a63f3245725a1f1e5cd9bea300f8d68f62b3de25a40968e`)
- Host payload check: `FWLIVE_REQUIRE_PACKAGE=1 FWLIVE_PACKAGE=... bash tests/fwlive-package-payload.test.sh` — passed for the current IPK (14 JS modules, ACL/menu files, libexec layout and modes).
- Host lifecycle check: `bash tests/fwlive-package-lifecycle.test.sh` — executes extracted `prerm-pkg` (an always-restore or old `$1=remove` body fails), the `default_prerm` `$1-pkg` hop, `PKG_UPGRADE=1` skip, literal-dot version glob (`1a2` must not restore), APK version-valued uninstall, staging guard, and best-effort restore.
- APK control inspection with OpenWrt 25.12 `apk adbdump` — custom `pre-deinstall` is present and contains the live-root guard and baseline restore call. APK 3.0 supplies the installed version to `pre-deinstall` for removal; it does not supply a literal `remove` action.

The two 24.10 `_all` IPK hashes are not a payload split. The inner package is
architecture-independent; each SDK build wraps that same payload in
per-build outer-archive metadata, so the x86_64 and aarch64_generic files
hash differently.

## Released v0.1.44 hook mismatch

Tag `v0.1.44` shipped `[ "$1" = "remove" ] || exit 0` in `prerm-pkg`. Through
the installed opkg wrapper, `$1` is the wrapper **path**, so that guard never
fired. In that release the `prerm` define also came *after*
`include $(TOPDIR)/feeds/luci/luci.mk`, which compounded the packaging gap.
Pre-#405 "#389 live opkg remove verified to restore" was taken against
a lab-planted `prerm` in `scripts/qemu-install-fwlive.sh` that #405 deleted.
Master-era lab installs with the planted always-restore script restore once on
the next version bump; tagged field users had the v0.1.44 non-matching body.
`docs/release.md` keeps `PKG_RELEASE:=1` and bumps the third octet at release,
so the fix ships at the next `v*` tag.

## Installed lifecycle checks

The strict checks start with no WAN `log` option and no baseline file, enable
logging through ubus, and then remove the package. Each uninstall step asserts
the WAN `log` bit and `/etc/fwlive/wan-log-baseline` state.

| Guest | Package manager | Result |
| --- | --- | --- |
| OpenWrt 24.10.8 armsr/armv8 (QEMU, port 2224; deliberate parallel-lab port, not the helper default 2222) | `opkg remove luci-app-fwlive` via `qemu-logging-uninstall-smoke.sh` | **PASS** (uninstall restoration) — uninstall only; no helper stdout token |
| OpenWrt 24.10.8 x86_64 (QEMU, port 2222) | `opkg remove luci-app-fwlive` | **PASS** (uninstall restoration) — remove restored the unset log and deleted the baseline |
| OpenWrt 25.12.5 x86_64 (QEMU, port 2223) | `apk del luci-app-fwlive` | **PASS** (uninstall restoration) — `pre-deinstall` restored the unset log and deleted the baseline |

### Same-version preservation checks (not hook proof)

These cells confirm that a no-op reinstall does not disturb an enabled WAN log
bit and baseline file. They do **not** exercise the skip-upgrade `prerm` body.
The discriminating inputs are `upgrade` and `PKG_UPGRADE=1`, which host tests on
#405 model against extracted `prerm-pkg`.

| Guest | Package manager | Result |
| --- | --- | --- |
| OpenWrt 24.10.8 x86_64 (QEMU, port 2222) | same-version `opkg install` (up-to-date) | **PASS** (no-op preservation) — opkg runs **no maintainer script** when already installed; preserved `log=1` and baseline are unchanged |
| OpenWrt 25.12.5 x86_64 (QEMU, port 2223) | `apk add --force-reinstall` | **PASS** (no-op preservation) — ran `post-upgrade` only (no `pre-deinstall`); preserved `log=1` and baseline |

On 25.12, `apk add --force-reinstall` emits
`Replacing ... (0.1.44-r1 -> 0.1.44-r1)` and runs `post-upgrade` without
running `pre-deinstall`; a real `apk del` runs `pre-deinstall` with the old
version. The hook therefore treats the version-shaped argument as the APK
uninstall signal and treats empty, unknown, and explicit upgrade actions as
fail-closed. This is not a claim about a cross-release 24.10-to-25.12
sysupgrade, which is not a supported package-manager operation.

The 24.10 IPK is `_all` and therefore architecture-independent. This is the
representative IPK format used for the supported 23.05/24.10 package surface;
the existing 23.05 x86 QEMU smoke coverage remains the runtime check for that
release. No separate 23.05 build was needed for this package-format check.
The QEMU installer does not overwrite package maintainer scripts, so these
checks exercise the scripts shipped by the IPK/APK artifacts.

### Armsr root-SSH observations (not session proof; not `qemu-logging-uninstall-smoke.sh`)

`qemu-logging-uninstall-smoke.sh` covers enable → `opkg remove` only. It does
**not** exercise `poll`, `resolve`, `rules`, or enable/disable. The following
root-SSH `ubus call fwlive ...` observations were recorded separately after
installing the pinned aarch64_generic IPK on the armsr guest. They are not
session-authenticated R8 proof and do not close R8.

Guest: OpenWrt 24.10.8 `r29233-443ec4032a`, target `armsr/armv8`, architecture
`aarch64_generic`, QEMU port 2224 (parallel lab; `qemu-logging-uninstall-smoke.sh`
and `scripts/run-openwrt-armsr-armv8-qemu.sh` default to 2222), one vCPU and
512 MiB RAM.

- `rules` returned `backend: nft` in about 25 seconds under TCG (slow but
  bounded; exit status 0).
- `poll` with a five-line request returned `messages_received: 5` and
  `effective_limit: 5`.
- `resolve` for `127.0.0.1` returned `localhost.lan`.
- `enable_wan_logging` and `disable_wan_logging` each returned `changed:true`;
  disable intentionally retained the baseline for a later uninstall or
  re-enable, and the subsequent `opkg remove` deleted it.

## Runtime prerequisite and bounded backend distinction

The stock 24.10.8 and 25.12.5 lab images contain `nft`/fw4 but do not contain
the `timeout` utility. This observation predates #761, which adds
`+coreutils-timeout` to `LUCI_DEPENDS`. At the time, manually installing the
official `coreutils-timeout` package removed `timeout_missing` and restored
`backend: nft`; the observation remains valid for the bare image, while a
normal fwlive install is expected to pull the provider automatically.

### #761 dependency verification (2026-09-26)

On an isolated copy of the prepared OpenWrt 24.10.8 x86_64 image
(`r29233-443ec4032a`), the guest started with fwlive 0.1.44-r1 and no
`timeout`. Installing the 0.1.46-r1 IPK upgraded fwlive and automatically
installed `coreutils` / `coreutils-timeout` 9.7-r1. `/usr/bin/timeout` resolved
to `/usr/libexec/timeout-coreutils`; `logging_status` had no warnings, poll
succeeded, `rules` reported `backend: nft`, and the required QEMU log-pipeline
smoke parsed three rows.

For the abnormal-state check, the timeout symlink was temporarily renamed on
that disposable guest. `logging_status` then returned `timeout_missing`, poll
returned `log_read_failed`, and `rules` returned `backend: unknown` with
`no_backend`. Live Playwright rendered “Firewall Live View is incomplete.
Reinstall luci-app-fwlive to restore it.”, not “Connection lost”. Restoring the
symlink cleared the warning. This exercises the missing-provider path without
removing the fail-closed command guard.
